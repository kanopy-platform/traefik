apiVersion: networking/v1
kind: Ingress
metadata:
  namespace: testing
spec:
  rules:
  - http:
      paths:
      - backend:
          service:
            name: service
            port: cheddar
        path: /cheddar
      - backend:
          service:
            name: service
            port: stilton
        path: /stilton
