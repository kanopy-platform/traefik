apiVersion: networking/v1
kind: Ingress
metadata:
  namespace: awesome
spec:
  rules:
  - host: foo
    http:
      paths:
      - backend:
          service:
            name: service1
            port: 80
        path: /foo
