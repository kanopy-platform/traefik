apiVersion: networking/v1
kind: Ingress
metadata:
  namespace: awesome
spec:
  rules:
  - http:
      paths:
      - backend:
          service:
            name: service1
            port: 801
        path: /bar
