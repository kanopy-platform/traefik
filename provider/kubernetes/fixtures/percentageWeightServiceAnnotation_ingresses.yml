apiVersion: networking/v1
kind: Ingress
metadata:
  annotations:
    ingress.kubernetes.io/service-weights: |2

      service1: 10%
      service3: 20%
  namespace: testing
spec:
  rules:
  - host: host1
    http:
      paths:
      - backend:
          service:
            name: service1
            port: "8080"
        path: /foo
      - backend:
          service:
            name: service2
            port: "7070"
        path: /foo
      - backend:
          service:
            name: service3
            port: "9090"
        path: /foo
      - backend:
          service:
            name: service2
            port: "7070"
        path: /bar
